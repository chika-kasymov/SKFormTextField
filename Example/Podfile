source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'SKFormTextField_Example', :exclusive => true do
  pod "SKFormTextField", :path => "../"
end

target 'SKFormTextField_Tests', :exclusive => true do
  pod "SKFormTextField", :path => "../"

  pod 'Specta'
  pod 'Expecta'
  pod 'FBSnapshotTestCase'
  pod 'Expecta+Snapshots'
end
